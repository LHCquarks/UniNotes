We define the complex logarithm as:
$$
\text{Log}(z) = \ln(|z|) + i \text{Arg}(z)
$$
We do this to preserve the property that:
$$
e^{\text{Log}(z)} = z
$$
Note:
	We do not have the property that $$\text{Log}(e^z) = z$$
