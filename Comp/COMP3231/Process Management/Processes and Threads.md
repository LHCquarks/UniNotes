## The problem
Almost all OS's want to run more processes than there are cores on a cpu which means that we need a way to allocate time to each process to run.
## What is a process
A process is the unit of resources used by one task. All processes get memory and cpu time.
